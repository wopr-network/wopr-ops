# GPU Production Node

> Operational facts for this node live in `GPU.md` (repo root). This file tracks OS-level and SSH details only.

**Status:** Not yet provisioned

## When Provisioned — Update This

| Field | Value |
|-------|-------|
| SSH User | root |
| SSH Key | (name of DO SSH key) |
| OS | Ubuntu 22.04 + NVIDIA drivers |
| cloud-init log | `/var/log/cloud-init-output.log` |
| Compose location | `/opt/wopr-gpu/docker-compose.gpu.yml` |
| Models location | `/opt/models/` |

## Notes

*(none yet)*
