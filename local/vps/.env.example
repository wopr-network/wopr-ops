# VPS machine environment — copy to local/vps/.env and fill in values.
#
# Usage:
#   cp local/vps/.env.example local/vps/.env
#   # Edit local/vps/.env with real values
#
# The real values for local dev live in ~/wopr-platform/.env on the host.
# Copy them over — they're the same stack.

# --- Database ---
POSTGRES_PASSWORD=wopr_local_dev

# --- Auth ---
BETTER_AUTH_SECRET=changeme_generate_with_openssl_rand_hex_32
PLATFORM_SECRET=changeme_generate_with_openssl_rand_hex_32
GPU_NODE_SECRET=changeme_generate_with_openssl_rand_hex_24
COOKIE_DOMAIN=localhost

# --- Stripe ---
STRIPE_SECRET_KEY=sk_test_REPLACE_ME
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME
STRIPE_DEFAULT_PRICE_ID=price_REPLACE_ME
STRIPE_CREDIT_PRICE_5=price_REPLACE_ME
STRIPE_CREDIT_PRICE_10=price_REPLACE_ME
STRIPE_CREDIT_PRICE_25=price_REPLACE_ME
STRIPE_CREDIT_PRICE_50=price_REPLACE_ME
STRIPE_CREDIT_PRICE_100=price_REPLACE_ME

# --- Email ---
RESEND_API_KEY=re_placeholder_local_dev
RESEND_FROM_EMAIL=noreply@localhost

# --- Cloudflare (optional) ---
CLOUDFLARE_API_TOKEN=

# --- Fleet ---
FLEET_API_TOKEN_WRITE=wopr_write_test_happypath_token_abc123

# --- GHCR (for pulling private images + Watchtower CD) ---
# GitHub PAT with read:packages scope
# Get one at: https://github.com/settings/tokens
REGISTRY_USERNAME=your-github-username
REGISTRY_PASSWORD=ghp_your_pat_with_read_packages
REGISTRY_SERVER=ghcr.io

# --- DigitalOcean (optional for local dev) ---
DO_API_TOKEN=local-dev-fake

# --- Watchtower (CD — auto-pull from GHCR on new image digest) ---
WATCHTOWER_POLL_INTERVAL=60

# --- Margin config (optional) ---
# MARGIN_CONFIG_JSON={"defaultMargin":1.3,"rules":[]}
